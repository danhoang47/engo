package layout

/*
1. Xác định Main Axis / Cross Axis.
2. Tính toán kích thước con (Measure).
3. Xử lý flex-grow, flex-shrink.
4. Xử lý justify-content (phân phối khoảng trống).
5/ Tính toán vị trí tuyệt đối (absolute).

Flexbox hoạt động theo dòng (Line)
*/
